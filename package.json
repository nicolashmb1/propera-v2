{
  "name": "propera-v2",
  "version": "0.1.0",
  "private": true,
  "description": "Propera V2 runtime (Node) — parallel to GAS; production cutover is later",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/*.test.js tests/scenarios/*.test.js",
    "test:integration": "node --test tests/integration/staffCaptureCrossChannel.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
